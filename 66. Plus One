#Python

class Solution:
    def plusOne(self, digits: List[int]) -> List[int]:
        f1=""
        for i in digits:
            f1 += str(i)+ ""
        n = int(f1)+1
        res = list(map(int, str(n)))
        return res
